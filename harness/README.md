# Harness (not published)

Local test surface for SPEC § 13 — real `createSession`, two source views, commands + inspector.

```bash
npm run harness
# → http://127.0.0.1:4173/
```

Behaviour tests drive `window.__harness.commands` (no pointer gestures).
